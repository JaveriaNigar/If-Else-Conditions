# TypeScript If‑Else Practice

A simple TypeScript exercise to practice conditional logic using `if`, `else if`, and `else` statements.

## 🧰 Technology Used  
- TypeScript  

## 📌 What You’ll Find  
- Example file demonstrating various `if‑else` scenarios  
- Comments explaining condition flows and decision‑making  

## 🚀 How to Run  
1. Clone the repository  
2. Navigate to the folder  
3. Run `tsc index.ts` (or your file name) to compile  
4. Open the compiled JS file in Node or browser console  
5. Experiment by changing conditions and values  
